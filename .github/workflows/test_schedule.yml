name: Test Schedule Trigger

on:
  schedule:
    # Runs every 5 minutes for testing
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
    - name: Print time
      run: |
        echo "Test schedule run: $(date -u)"
